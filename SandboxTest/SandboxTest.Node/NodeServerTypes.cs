namespace SandboxTest.Node
{
    /// <summary>
    /// Enum that represents the web servers that the node application runner can run.
    /// </summary>
    public enum NodeServerTypes
    {
        WebPack,
        Vite,
        Custom
    }
}
